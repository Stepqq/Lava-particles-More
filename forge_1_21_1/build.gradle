plugins {
    id 'net.minecraftforge.gradle' version '6.0.+'
    id 'eclipse'
    id 'maven-publish'
}

version = '1.0.0'
group = 'com.lavalive'
archivesBaseName = 'lavalive-forge-1.21.1'

java.toolchain.languageVersion = JavaLanguageVersion.of(21)

println "Java: ${System.getProperty('java.version')}, JVM: ${System.getProperty('java.vm.version')} (${System.getProperty('java.vendor')}), Arch: ${System.getProperty('os.arch')}"
minecraft {
    mappings channel: 'official', version: '1.21.1'

    runs {
        client {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            property 'forge.enabledGameTestNamespaces', 'lavalive'
            mods {
                lavalive {
                    source sourceSets.main
                }
            }
        }
    }
}

sourceSets {
    main {
        java {
            srcDirs = [
                'src/main/java',
                '../common/src/main/java'
            ]
        }
        resources {
            srcDirs = [
                'src/main/resources',
                '../common/src/main/resources'
            ]
        }
    }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.21.1-52.0.10'
}

tasks.named('processResources', ProcessResources).configure {
    var replaceProperties = [
        minecraft_version: '1.21.1',
        minecraft_version_range: '[1.21.1, 1.22)',
        forge_version: '52.0.10',
        forge_version_range: '[52,)',
        loader_version_range: '[52,)',
        mod_id: 'lavalive',
        mod_name: 'Lava Particles | More',
        mod_license: 'LGPL-3.0-only',
        mod_version: '1.0.0',
        mod_authors: 'stepqq',
        mod_description: 'An atmospheric mod making Minecraft lava feel alive with cinematic bubbling and popping.'
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/mods.toml', 'pack.mcmeta']) {
        expand replaceProperties
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}
